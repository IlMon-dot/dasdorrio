# DESIGN.md — визуальная система сайта-прокладки Dasdorrio

Записано по построенному экрану (`index.html`, сентябрь 2026). Это
источник правды для любой правки визуала. Данные товаров — только в
`products.js`.

## Мир

**Сцена товара.** Один экран, товар живёт на сцене в собственной палитре,
как расцветка кроссовка на странице релиза: сменил товар — сменился весь
мир. Лента выбора справа заимствует грамматику ленты коротких видео, из
которой человек пришёл: вертикальный ряд круглых кнопок под большой палец.
Ничего не объясняем, ничего не продаём — узнал и ушёл по таблетке площадки.

Что мир отказывается делать: сетка одинаковых карточек, список ссылок в
столбик, белый фон с серым текстом, «подзаголовок-кикер» над заголовком,
градиентный текст, стекло, эмодзи вместо иконок.

## Режим

Persuade с дисциплиной Operate: сцена ведёт, но действие (таблетки площадок)
всегда видно без прокрутки на 360–430 × 640–930 px.

## Токены

### Палитра сцены (меняется с товаром, живёт в `products.js`)

| Токен | Роль | Правило |
|---|---|---|
| `--bg` | фон сцены | цвет с самого товара, насыщенный, темнее или светлее героя минимум на 0,15 по относительной яркости, чтобы товар отделялся |
| `--bg2` | тень сцены | тот же тон, темнее на один шаг; радиальный свет снизу |
| `--accent` | первый акцент | цвет детали товара; выделение текста, кольцо фокуса |
| `--accent2` | второй акцент | цвет второй детали |
| `--fg` | текст | ≥ 4,5:1 к `--bg` и к `--bg2` (проверено на всех пяти) |
| `--fg2` | вторичный текст | тот же тон, что `--fg`, ≥ 4,5:1 к `--bg`; никогда не серый |
| `логотип` | версия знака | `светлый` на тёмных сценах, `тёмный` на светлых |

Текущие пять: багги `#1B2620` / `#F4F8F2`, адаптер `#F26A1B` / `#1B1006`,
дракон `#0B7C69` / `#FFFFFF`, медведь `#F0A500` / `#3B1E06`,
монстр `#2E7D22` / `#FFFFFF`.

### Цвета площадок (не меняются)

Wildberries `#CB11AB`, Ozon `#005BFF`, Яндекс Маркет `#FFCC00`, магазин
dasdorrio.ru — `--accent` палитры. С сентября 2026 они живут только в
**монетке** таблетки (кружок 12 px слева от названия): сама таблетка
полупрозрачная в тоне сцены — `rgba(0,0,0,.26)` на тёмной сцене,
`rgba(255,255,255,.34)` на светлой (`--pill-bg`, ставится скриптом по полю
`логотип`), обводка 1 px `--pill-line`, размытие фона под таблеткой 8 px,
текст `--fg`. Контраст текста на таблетке выше, чем на голой сцене, потому
что тон уводит фон от цвета текста.

### Родная сцена (с 2026-09-11, вечер)

У каждого товара одна **цельная картинка** его мира, сгенерированная по
референсам с исходников владельца (Higgsfield, Nano Banana, 2K): багги на
осенней лесной дороге, адаптер на глянцевом оранжевом с ноутбуком, наборы в
праздничной гостиной с ребёнком, машинка с шариком вылетает из насоса. Из
неё делаются две вещи: `сцена` — та же картинка с дорисованным фоном вместо
товара (`photo/bg/<ключ>.webp`, квадрат 1400 px), и `герой` — вырезка товара
из того же кадра. `геройБокс` = [x, y, w, h] в долях квадрата: герой лежит
ровно там, где стоял в кадре, поэтому свет, перспектива и масштаб совпадают.

Механика кадра (`.layer`): под всем — размытая копия сцены во весь экран
(`.blur`, 30 px, яркость 0,86); поверх — резкий квадрат `.frame` размером
`--cover-size` (телефон: 100vw, десктоп: min(112dvh, 100vw)) с центром
`--cover-cx` (48 % / 60 %) и `--cover-cy`, который скрипт ставит так, чтобы
центр героя совпал с центром подиума. Скрипт же ограничивает квадрат сверху
(`--cover-max`): герой не выше 92 % подиума, иначе в высоких окнах он налезал
бы на название. На десктопе кадр обычно меньше окна, поэтому все четыре его
края уходят в размытую заливку (две маски-градиента, `mask-composite: intersect`),
на телефоне — только верх и низ. Края квадрата уходят в размытие
маской (телефон — сверху и снизу 5 %, десктоп — слева и справа 10 %).
Между фоном и героем лежит вуаль `.veil` — градиент цвета `фон` палитры:
снизу плотный под текст, сверху лёгкий под логотип. Герой и его тень живут
в отдельном переднем кадре `.frame.fg` без маски и **над** вуалью, поэтому
товар не тускнеет, а пол вокруг него уходит в тень — светлый насос не
сливается с фоном. Тон `фон` у наборов глубокий (тёмно-бирюзовый,
коричневый, тёмно-зелёный), а не цвет самого насоса.

Как получать вырезку без людей: модель отказывается удалять ребёнка из
кадра, поэтому герой наборов собирается из двух масок — мягкая альфа
`remove_background` с цельного кадра, пропущенная через маску разницы
«цельная − фон без товара» (отсекает руки и шары на полу, не тянет тень),
плюс сама маска разницы для летящей машинки с шариком. У дракона ворота —
выпуклая оболочка компонента разницы (светлая ручка на светлом окне
проваливается под порог), у насоса остаётся только крупнейшая связная часть,
шарик добирается сегментацией ISNet. Сами маски насоса и машинки снимаются
с увеличенных кропов (×3 и ×2) без ворот по разнице (они срезали кончик
ручки), а цвет на мягком крае пересчитывается по
дорисованному фону и затем берётся с ближайшего плотного пикселя товара
(edge extend), край уплотнён: под героем лежит тёмная вуаль, и край,
подобранный под светлый пол, иначе светится. Сцены одной линейки снимаются с одной
дистанции: готовая сцена соседа — первый референс композиции. Скрипты —
`assemble_v3.py` и `masks_v4.py` в scratchpad сессии.

### Типографика

- Display: **Unbounded 800** — название товара. Мобильный
  `clamp(23px, 6.4vw, 34px)`, десктоп `clamp(34px, 3.4vw, 54px)`,
  ≥ 1600 px — 58px. Межстрочный 1,06, трекинг −0,02em, `text-wrap: balance`,
  переносы только ручные: слово никогда не рвётся посреди.
- Текст: **Onest 500** — таблетки (16/17 px). Мельче 14 px не бывает.
  Подписи под названием и подвала нет — убраны по просьбе владельца.
- Запасные: Arial Black / системный sans. Google Fonts, `display=swap`.

### Форма

- Таблетки: высота 52 px (десктоп 56), радиус 999, отступы 14/20 px,
  полупрозрачная заливка в тоне сцены, монетка площадки 12 px слева.
- Миниатюры ленты: круг 60 px (десктоп 76, низкие экраны 50), фон — `фон`
  своего товара, высветленный на 22 % белым; миниатюра — товар **целиком**,
  а у наборов — **только насос** (самая узнаваемая деталь, по просьбе
  владельца), вписан в 90–92 % круга, 200×200. Активная: масштаб
  1,14 и двойное кольцо `--bg` / `--fg`. У ленты поля 12 px со всех сторон:
  лента прокручиваемая, и поля уже кольца режут его.
- Герой: бокс 92 × 90 % подиума, `object-fit: contain`; тень — эллипс
  радиальным градиентом, ставится скриптом под реальный низ картинки.
- Герой: вырезка из родной сцены на своём месте (см. выше). В покое
  подпрыгивает: y −14 px за 0,42 с power2.out, обратно 0,5 с bounce.out,
  пауза 1,6 с; тень под ним (эллипс в `геройБокс`) сжимается до 0,86.
  Декора нет.
- Отступы: поле 20 px (десктоп 56), логотип 118 px (десктоп 168), верх сцены
  72 px + safe-area, низ 20 px + safe-area.

### Раскладка

- Телефон (< 900 px): столбец — логотип, подиум (1fr), текст + таблетки
  (auto). Подиум отступает справа на ширину ленты. Лента —
  `position: absolute`, центр по вертикали совпадает с центром подиума
  (скрипт `placeRail`), поэтому никогда не ложится на заголовок.
- Десктоп (≥ 900 px): постер — резкий квадрат сцены с центром на 60 %
  ширины, название и таблетки внизу слева (ширина до 540 px),
  подиум `min(38vh, 400px)`, лента у правого края.
- Горизонтальной прокрутки нет ни на одной ширине; вертикальной на
  360 × 640 и выше — тоже.

## Движение (GSAP 3.12, CDN)

Один авторский момент — **смена сцены**:

| Что | Как |
|---|---|
| палитра | `gsap.to(:root, {--bg…})` 0,6 с power2.inOut |
| старый герой | x −140·dir, поворот −6°, масштаб 0,92, прозрачность 0, 0,38 с power2.in |
| старый декор | масштаб 0, 0,28 с, каскад 0,03 |
| новый герой | из x +160·dir, поворот 6°, 0,62 с power3.out, старт на 0,22 с |
| новый декор | из масштаба 0, 0,55 с back.out(1.8), каскад 0,06, старт 0,38 с |
| название | y 16 → 0, 0,45 с power2.out |
| таблетки | y 16 → 0 и scale .96 → 1, back.out(1.6), каскад 0,05; после твина inline-стили снимаются (clearProps), наведение — CSS :hover −3 px |
| кружки ленты (первая отрисовка) | x 24 → 0, opacity 0 → 1, power3.out, каскад 0,06, задержка 0,35; clearProps |
| подсказка «Выберите товар» | один раз на устройстве (localStorage `dasdorrio-hint`): под лентой, стрелка вверх, появляется через 1,4 с, покачивается к кружкам, исчезает через 5,5 с или после первого касания ленты |
| таблетки | y 16 → 0, масштаб 0,96 → 1, back.out(1.6), каскад 0,05 |
| покой | герой y ±8 px 3,2 с sine.inOut yoyo; декор ±10–12 px и ±4° с разными периодами 2,6–4,4 с |
| таблетка под пальцем | масштаб 0,94 → 1, elastic.out(1, .45), 0,5 с; заливка плотнее |
| смена сцены | новый слой 0 → 1 за 0,75 с power2.out, герой из y 48·dir и масштаба 0,94 за 0,7 с power3.out, старый слой гаснет за 0,5 с с задержкой 0,3 с |
| таблетка под курсором | y −4, back.out(3), 0,45 с (только hover-устройства) |

Правила: анимируем только transform и opacity; таймлайн перебивается
следующим тапом (`kill`), кадр от опоздавшего preload пропускается;
`prefers-reduced-motion` — только прозрачность 0,25–0,35 с, парения нет.
Направление вылета зависит от того, выше или ниже в ленте новый товар.

## Поверхности браузера

`::selection` — `--accent` на `--bg`; `:focus-visible` — 3 px `--accent`,
отступ 3 px, скруглённый; полоса прокрутки ленты скрыта, прокрутка
остаётся; `theme-color` меняется вместе со сценой; серая вспышка тапа
выключена.

## Доступность

Лента: кнопки с `aria-label` названия и `aria-current` у активной, стрелки
вверх/вниз переключают товар. Герой — `alt` с названием, декор —
`aria-hidden`. Таблетки — настоящие ссылки `<a>` с `rel="noopener"`; подпись,
адрес и цвет монетки — из списка `ссылки` в products.js (правится в edit.html). Хэш `#ключ` в адресе даёт прямую ссылку на товар, «назад»
работает.

## Что не делать

Не добавлять секции ниже сцены, цены, отзывы, попапы, таймеры, автопрокрутку
ленты. Не менять цвета площадок. Не ставить hover-эффекты, которых нет по
тапу. Не грузить декор тяжелее 40 КБ и героя тяжелее 150 КБ.
