# DESIGN.md — визуальная система сайта-прокладки Dasdorrio

Записано по построенному экрану (`index.html`, сентябрь 2026). Это
источник правды для любой правки визуала. Данные товаров — только в
`products.js`.

## Мир

**Сцена товара.** Один экран, товар живёт на сцене в собственной палитре,
как расцветка кроссовка на странице релиза: сменил товар — сменился весь
мир. Лента выбора справа заимствует грамматику ленты коротких видео, из
которой человек пришёл: вертикальный ряд круглых кнопок под большой палец.
Ничего не объясняем, ничего не продаём — узнал и ушёл по таблетке площадки.

Что мир отказывается делать: сетка одинаковых карточек, список ссылок в
столбик, белый фон с серым текстом, «подзаголовок-кикер» над заголовком,
градиентный текст, стекло, эмодзи вместо иконок.

## Режим

Persuade с дисциплиной Operate: сцена ведёт, но действие (таблетки площадок)
всегда видно без прокрутки на 360–430 × 640–930 px.

## Токены

### Палитра сцены (меняется с товаром, живёт в `products.js`)

| Токен | Роль | Правило |
|---|---|---|
| `--bg` | фон сцены | цвет с самого товара, насыщенный, темнее или светлее героя минимум на 0,15 по относительной яркости, чтобы товар отделялся |
| `--bg2` | тень сцены | тот же тон, темнее на один шаг; радиальный свет снизу |
| `--accent` | первый акцент | цвет детали товара; выделение текста, кольцо фокуса |
| `--accent2` | второй акцент | цвет второй детали |
| `--fg` | текст | ≥ 4,5:1 к `--bg` и к `--bg2` (проверено на всех пяти) |
| `--fg2` | вторичный текст | тот же тон, что `--fg`, ≥ 4,5:1 к `--bg`; никогда не серый |
| `логотип` | версия знака | `светлый` на тёмных сценах, `тёмный` на светлых |

Текущие пять: багги `#1B2620` / `#F4F8F2`, адаптер `#F26A1B` / `#1B1006`,
дракон `#0B7C69` / `#FFFFFF`, медведь `#F0A500` / `#3B1E06`,
монстр `#2E7D22` / `#FFFFFF`.

### Цвета площадок (не меняются)

Wildberries `#CB11AB`, Ozon `#005BFF`, Яндекс Маркет `#FFCC00`, магазин
dasdorrio.ru — `--accent` палитры. С сентября 2026 они живут только в
**монетке** таблетки (кружок 12 px слева от названия): сама таблетка
полупрозрачная в тоне сцены — `rgba(0,0,0,.26)` на тёмной сцене,
`rgba(255,255,255,.34)` на светлой (`--pill-bg`, ставится скриптом по полю
`логотип`), обводка 1 px `--pill-line`, размытие фона под таблеткой 8 px,
текст `--fg`. Контраст текста на таблетке выше, чем на голой сцене, потому
что тон уводит фон от цвета текста.

### Сцена-картинка

Под палитрой лежит `сцена` из `products.js` (`photo/bg/<ключ>.webp`, квадрат
1400 px, ≤ 120 КБ): мир товара — трасса из видео для багги, дуги сигнала для
адаптера, боке шариков в палитре для наборов. Поверх — `.tint`: линейный
градиент палитры (снизу сплошной `--bg` до 6 %, 82 % на 30 %, 45 % на 62 %,
30 % сверху) плюс радиальная тень `--bg2` снизу. Картинка грузится после
героя и появляется кроссфейдом 0,6–0,9 с; старая гаснет за 0,7 с. Нет поля —
сцена одноцветная.

### Типографика

- Display: **Unbounded 800** — название товара. Мобильный
  `clamp(23px, 6.4vw, 34px)`, десктоп `clamp(34px, 3.4vw, 54px)`,
  ≥ 1600 px — 58px. Межстрочный 1,06, трекинг −0,02em, `text-wrap: balance`,
  переносы только ручные: слово никогда не рвётся посреди.
- Текст: **Onest 400/600** — подпись (15/18 px), таблетки (16/17 px, 600),
  подвал 14 px. Мельче 14 px не бывает.
- Запасные: Arial Black / системный sans. Google Fonts, `display=swap`.

### Форма

- Таблетки: высота 52 px (десктоп 56), радиус 999, отступы 14/20 px,
  полупрозрачная заливка в тоне сцены, монетка площадки 12 px слева.
- Миниатюры ленты: круг 56 px (десктоп 72, низкие экраны 48), фон — `фон`
  своего товара, картинка 76 %, активная: масштаб 1,14 и двойное кольцо
  `--bg` / `--fg`.
- Герой: бокс 92 × 90 % подиума, `object-fit: contain`; тень — эллипс
  радиальным градиентом, ставится скриптом под реальный низ картинки.
- Декор: до пяти деталей в фиксированных гнёздах (22 / 19 / 18 / 16 / 13 %
  ширины подиума), лёгкая `drop-shadow`.
- Отступы: поле 20 px (десктоп 56), логотип 118 px (десктоп 168), верх сцены
  72 px + safe-area, низ 20 px + safe-area.

### Раскладка

- Телефон (< 900 px): столбец — логотип, подиум (1fr), текст + таблетки
  (auto). Подиум отступает справа на ширину ленты. Лента —
  `position: absolute`, центр по вертикали совпадает с центром подиума
  (скрипт `placeRail`), поэтому никогда не ложится на заголовок.
- Десктоп (≥ 900 px): сетка 11fr / 13fr — слева название, подпись, таблетки;
  справа подиум высотой `min(72vh, 760px)`; лента у правого края.
- Горизонтальной прокрутки нет ни на одной ширине; вертикальной на
  360 × 640 и выше — тоже.

## Движение (GSAP 3.12, CDN)

Один авторский момент — **смена сцены**:

| Что | Как |
|---|---|
| палитра | `gsap.to(:root, {--bg…})` 0,6 с power2.inOut |
| старый герой | x −140·dir, поворот −6°, масштаб 0,92, прозрачность 0, 0,38 с power2.in |
| старый декор | масштаб 0, 0,28 с, каскад 0,03 |
| новый герой | из x +160·dir, поворот 6°, 0,62 с power3.out, старт на 0,22 с |
| новый декор | из масштаба 0, 0,55 с back.out(1.8), каскад 0,06, старт 0,38 с |
| название, подпись | y 16 → 0, 0,45 с power2.out, каскад 0,06 |
| таблетки | y 16 → 0, масштаб 0,96 → 1, back.out(1.6), каскад 0,05 |
| покой | герой y ±8 px 3,2 с sine.inOut yoyo; декор ±10–12 px и ±4° с разными периодами 2,6–4,4 с |
| таблетка под пальцем | масштаб 0,94 → 1, elastic.out(1, .45), 0,5 с; заливка плотнее |
| смена сцены-картинки | новая 0 → 1 за 0,9 с power2.out, старая 1 → 0 за 0,7 с с задержкой 0,2 с |
| таблетка под курсором | y −4, back.out(3), 0,45 с (только hover-устройства) |

Правила: анимируем только transform и opacity; таймлайн перебивается
следующим тапом (`kill`), кадр от опоздавшего preload пропускается;
`prefers-reduced-motion` — только прозрачность 0,25–0,35 с, парения нет.
Направление вылета зависит от того, выше или ниже в ленте новый товар.

## Поверхности браузера

`::selection` — `--accent` на `--bg`; `:focus-visible` — 3 px `--accent`,
отступ 3 px, скруглённый; полоса прокрутки ленты скрыта, прокрутка
остаётся; `theme-color` меняется вместе со сценой; серая вспышка тапа
выключена.

## Доступность

Лента: кнопки с `aria-label` названия и `aria-current` у активной, стрелки
вверх/вниз переключают товар. Герой — `alt` с названием, декор —
`aria-hidden`. Таблетки — настоящие ссылки `<a>` с `rel="noopener"`, текст
площадки внутри. Хэш `#ключ` в адресе даёт прямую ссылку на товар, «назад»
работает.

## Что не делать

Не добавлять секции ниже сцены, цены, отзывы, попапы, таймеры, автопрокрутку
ленты. Не менять цвета площадок. Не ставить hover-эффекты, которых нет по
тапу. Не грузить декор тяжелее 40 КБ и героя тяжелее 150 КБ.
